{
  "name": "login-reactjs-nodejs",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "concurrently  \"cd server && npm start\" \"cd client && npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Seemab",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.0.0"
  }
}
